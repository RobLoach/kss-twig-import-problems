{#
/**
 * @file
 * Tag component.
 *
 * Available variables:
 * - url: url to the tag listing page.
 * - name: name of the tag.
 */
#}
{{ attach_library('mytheme/tag') }}
<a href="{{ url }}" class="tag">{{ name }}</a>
